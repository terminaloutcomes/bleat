name: Build bleat-api container

on:
  push:
    branches:
      - main
    tags:
      - "v*"
    paths:
      - .dockerignore
      - bleat-api/**
      - .github/workflows/build-bleat-api-container.yml
  pull_request:
    paths:
      - .dockerignore
      - bleat-api/**
      - .github/workflows/build-bleat-api-container.yml

permissions:
  contents: read

jobs:
  build:
    uses: docker/github-builder/.github/workflows/build.yml@58cb9f5b71b1836d6f690c1e95effdeb9b98cb8a # v1
    permissions:
      contents: read
      id-token: write
      packages: write
    with:
      cache: true
      cache-mode: max
      build-args: |
        BLEAT_API_IMAGE_NAME=ghcr.io/${{ github.repository_owner }}/bleat-api
        BLEAT_API_IMAGE_VERSION={{meta.version}}
      context: .
      file: bleat-api/Dockerfile
      output: image
      platforms: linux/amd64,linux/arm64
      push: ${{ github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository }}
      sbom: true
      target: runtime
      meta-images: ghcr.io/${{ github.repository_owner }}/bleat-api
      meta-tags: |
        type=raw,value=build-{{date 'YYYYMMDD-HHmmss'}}
        type=raw,value=latest,enable=${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
        type=semver,pattern={{version}},enable=${{ startsWith(github.ref, 'refs/tags/v') }}
        type=raw,value=${{ github.sha }},enable=${{ github.event_name == 'pull_request' }}
    secrets:
      registry-auths: |
        - registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
